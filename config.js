// REID'S PARTY — shared config for index.html (form) and admin.html (dashboard)
//
// ENDPOINT = your Google Apps Script Web App URL (ends in /exec).
// See SETUP.md — until this is set, the live site can't accept submissions.
// When empty and running on localhost, the pages automatically use the local
// mock server (mock_server.py) at /exec so you can demo everything offline.
window.PARTY_CONFIG = {
  ENDPOINT: ""
};
